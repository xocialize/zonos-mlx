"""Port of upstream ``zonos/utils.py`` — misc helpers.

STATUS: skeleton (Step 3). Translate only the helpers actually used by the torch
backbone path; drop device/dtype plumbing that is torch-specific.
"""

# TODO(Step 3): translate helpers as referenced during module translation.
